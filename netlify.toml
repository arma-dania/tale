[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Pensumarket og læringsmålene læses af eksaminator-funktionen og skal
# derfor pakkes med ud i funktionen.
[functions]
  included_files = ["src/pensum/**"]
